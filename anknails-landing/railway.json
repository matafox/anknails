{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "pip install -r backend/requirements.txt && python -m uvicorn backend.app:app --host 0.0.0.0 --port $PORT"
  }
}
